# Examples

Run the Vite dev server and open the example pages:

```
pnpm dev
```

- Todo app: `/examples/todo/`
- Combobox + floating-ui: `/examples/combobox/`

Each example registers a local adapter (see `examples/shared/store.ts`) for a minimal signal implementation.
